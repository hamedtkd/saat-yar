# فاز ۶ — بودجه، هزینه و سود پروژه

در این فاز لایه مالی پروژه‌های فریلنسری تکمیل شد:

- ثبت و حذف هزینه برای هر پروژه
- دسته‌بندی هزینه به نرم‌افزار، پیمانکار، رفت‌وآمد، تجهیزات و سایر
- محاسبه درآمد واقعی از رکوردهای قابل صورتحساب و نرخ مؤثر هر رکورد
- محاسبه مجموع هزینه، سود خالص و حاشیه سود
- هشدار مصرف ۸۰٪ بودجه زمانی و عبور از ۱۰۰٪
- نمایش وضعیت بودجه و سود در فهرست و جزئیات پروژه
- رعایت حالت سراسری مخفی‌کردن اطلاعات مالی
- ارتقای Schema از نسخه ۸ به ۹ و Migration خودکار `expenses: []`
- ادغام هزینه‌ها هنگام Restore به روش Merge

## مدل داده جدید

```ts
type Expense = {
  id: string;
  projectId: string;
  clientId: string;
  title: string;
  amount: number;
  date: string;
  category: "software" | "contractor" | "travel" | "equipment" | "other";
  note?: string;
  createdAt: string;
};
```

هزینه‌ها Local-first هستند و همراه Backup ذخیره و بازیابی می‌شوند.
