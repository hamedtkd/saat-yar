# فاز ۳۳ — انتشار پایدار ۲.۰.۰

این فاز نسخه بازطراحی‌شده ساعت‌یار را برای انتشار پایدار آماده می‌کند.

## اصلاحات نهایی

- تست Backup دیگر شماره Schema را hard-code نمی‌کند و از `APP_DATA_SCHEMA_VERSION` استفاده می‌کند.
- فایل‌های legacy زیر در خروجی وجود ندارند و تست معماری از بازگشت آن‌ها جلوگیری می‌کند:
  - `app/date-time-pickers.tsx`
  - `app/storage.ts`
- نسخه پکیج به `2.0.0` ارتقا یافت.
- چک‌لیست انتشار فارسی اضافه شد.

## محدوده نسخه ۲

- معماری App Router و Local-first
- Migration نسخه‌بندی‌شده تا Schema ۱۳
- برنامه کاری هفتگی و شیفت شب
- تعطیلات رسمی و Override دستی
- موتور حقوق، مزایا و کسورات
- هزینه، سود، بودجه پروژه و فاکتور
- Privacy Mode سراسری
- Theme روشن، تاریک، System و Custom
- بازطراحی Spotify-inspired
- Sidebar دسکتاپ و Bottom Navigation موبایل
- Accessibility و کنترل کیفیت سراسری
- قانون حداکثر ۲۵۰ خط برای فایل‌های Production
