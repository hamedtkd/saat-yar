# فاز ۸۱ — بازگردانی کوتاه‌مدت حذف رکورد

- پس از تأیید حذف، یک Snapshot مستقل از رکورد نگهداری می‌شود.
- کاربر ۱۰ ثانیه فرصت دارد رکورد حذف‌شده را کامل بازگرداند.
- Snapshot شامل وقفه‌ها نیز به‌صورت کپی مستقل است.
- با بازگردانی، تاریخ همان رکورد انتخاب و زمان به‌روزرسانی ثبت می‌شود.
- پیام Undo دارای `role=status` و `aria-live=polite` است.
- هیچ Migration یا Dependency جدیدی اضافه نشده است.
