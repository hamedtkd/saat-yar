# فاز ۴۹ — سازگاری تست‌ها با TypeScript Target

## هدف

رفع خطای `TS1501` در تست پایداری یادآوری استراحت بدون تغییر `tsconfig` یا پایین‌آوردن کیفیت بررسی معماری.

## تغییرات

- حذف flag جدیدتر `s` از عبارت منظم تست فاز ۴۸.
- جایگزینی dot-all با الگوی سازگار `[\s\S]*?`.
- اضافه‌شدن تست رگرسیون برای جلوگیری از بازگشت regex ناسازگار با target فعلی TypeScript.
- اضافه‌شدن تست فاز ۴۹ به دستور اصلی `npm test`.

## نتیجه مورد انتظار

`npm run typecheck` دیگر روی `tests/phase48-reminder-stability.test.ts` خطای `TS1501` نمی‌دهد و رفتار تست قبلی حفظ می‌شود.

## Migration

- نسخه Schema: ۱۴
- Migration جدید: ندارد
- Dependency جدید: ندارد
