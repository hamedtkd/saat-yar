# فاز ۲۰ — ریفکتور نمای ماه

در این فاز جدول جزئیات ماه و نمودار هفتگی به ماژول‌های کوچک‌تر تقسیم شدند.

## اهداف

- نگه‌داشتن هر فایل زیر ۲۵۰ خط
- جداسازی محاسبات از JSX
- اشتراک‌گذاری نمایش تراز روز
- استفاده از `SurfaceCard` و `EmptyState`
- پوشش معماری برای جلوگیری از بازگشت فایل‌های بزرگ

## ساختار جدید

- `components/pages/month/table/*`
- `components/pages/month/weekly-chart/*`
- `tests/month-view-architecture.test.ts`

این فاز تغییری در Schema یا داده‌های کاربران ندارد.
